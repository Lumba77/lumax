# Tests (manual / ad hoc)

Small **manual** scripts used from the repo root while developing (STT/TTS smoke tests). They are not a full pytest suite.

Run from repo root (adjust Python path if needed):

```bash
python tests/test_stt_backend.py
```

Move or add **automated** tests here over time; keep Mind/Cognition tests that already live under `Backend/Mind/` unless you consolidate later.
